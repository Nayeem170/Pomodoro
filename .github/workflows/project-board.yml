name: Project Board - Issues Only

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  enforce-issue-link:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR body for issue reference
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BODY=$(gh pr view ${{ github.event.pull_request.number }} --json body --jq '.body')
          if echo "$BODY" | grep -qiP '(closes|fixes|resolves)\s+#\d+'; then
            echo "linked=true" >> $GITHUB_OUTPUT
            echo "PR is linked to an issue."
          else
            echo "linked=false" >> $GITHUB_OUTPUT
            echo "PR is NOT linked to an issue."
          fi

      - name: Comment on PR if no issue linked
        if: steps.check.outputs.linked == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr comment ${{ github.event.pull_request.number }} --body "⚠️ **This PR is not linked to an issue.**

          Every PR must be part of an issue card on the project board.

          Please update the PR body to include an issue reference:
          - \`Closes #XX\`
          - \`Fixes #XX\`
          - \`Resolves #XX\`

          If no issue exists yet, [create one](https://github.com/Nayeem170/Pomodoro/issues/new) first."

      - name: Remove PR from project board
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PROJECT_ITEMS=$(gh api graphql -f query='
            query($owner: String!, $number: Int!) {
              user(login: $owner) {
                projectV2(number: $number) {
                  items(first: 100) {
                    nodes {
                      id
                      content {
                        ... on PullRequest { number }
                      }
                    }
                  }
                }
              }
            }
          ' -f owner="${{ github.repository_owner }}" -f number=4 --jq '.data.user.projectV2.items.nodes[] | select(.content.number == '"${{ github.event.pull_request.number }}"') | .id')

          for ITEM_ID in $PROJECT_ITEMS; do
            gh api graphql -f query='
              mutation($projectId: ID!, $itemId: ID!) {
                deleteProjectV2Item(input: { projectId: $projectId, itemId: $itemId }) {
                  deletedItemId
                }
              }
            ' -f projectId="PVT_kwHOAJBk4M4BWD1D" -f itemId="$ITEM_ID"
            echo "Removed PR #${{ github.event.pull_request.number }} from project board"
          done
